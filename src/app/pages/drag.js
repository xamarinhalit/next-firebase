import React from 'react';
import { Drag } from '../components';
import { connect } from 'react-redux';
class _DragIndex extends React.Component {
  render () {
      return (<Drag></Drag>);
  }
}
export default connect(
  null,
  null,
)(_DragIndex)
